### That is a simple script to convert .vtt subtitles in a directorectory to .srt

<br/>

**you need to accomplish this steps:**<br/>

`./convert_vtt_to_srt.py <directory_path>` or `./convert_vtt__to_srt.py <filename.vtt>`
